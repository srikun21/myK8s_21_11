apiVersion: v1
kind: service
metadata:
  name: myjenkins-svc
  labels:
     app: myjekins-pod
spec:
  type: Nodeport
  ports:
  - port: 8080
    nodeport: 30001
    protocol: TCP
  selector:
      app:  myjekins-pod
